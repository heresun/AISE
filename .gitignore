# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Logs / temp
*.log
*.bak
*.tmp

# AISE 运行时产物（每个使用方项目的 plan/runs/metrics 目录，不入插件库）
.aise/
.pytest_cache/

# Java/Maven + Rust 构建产物（fixture 跑 mvn test / cargo test 后产生）
target/

# Node/Jest 依赖（fixture 跑 npm install 后产生）
node_modules/
package-lock.json

# Rust Cargo.lock（lib fixture 不入库 lock，避免锁版本）
Cargo.lock
